<div class="container">
  <div class="row justify-content-center">
   <div class="col-md-8">
      <h1>Create a new cocktail</h1>
        <%= simple_form_for(@cocktail) do |f| %>
        <%= f.input :name %>
        <%= f.submit "Submit cocktail", class: "btn btn-primary"%>
        <% end %>
<%= link_to "Back to all cokctails", :root %>
      </div>
  </div>
</div>

